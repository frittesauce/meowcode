fn main(test) {
    let meow = 1;
    let woof = 2;

    chirp("Hello World");

    if (meow == woof) {
        chirp(false);
    } else {
        chirp("skibidi skiidi on the wall whos the skibidies of them all");
    }
}
